Jan 7th

Initialized Minikubeon Windows 11 using docker (better use docker instead of VM as you would need to do some bios changes)

started Minikube with 2 CPU - 4gb ram and 20gb disk space (modifiable)

enabled ingress controller and verified if ingress-nginx-ontroller was running in its namespace

Applied kubernetes example ingress and debugged a missing backend service (8080) by creating temp pod and exposing as service

Validated and running

Cmd history and command file in log files